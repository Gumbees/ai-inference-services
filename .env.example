# ============================================
# AI Inference Services - Environment Configuration
# ============================================
# Copy this file to .env and configure your values

# --------------------------------------------
# Traefik Domain Configuration
# --------------------------------------------

# Whisper (Speech-to-Text)
WHISPER_PUBLIC_DOMAIN=whisper.example.com
WHISPER_PRIVATE_DOMAIN=whisper.local.example.com

# Ollama (LLM Inference)
OLLAMA_PUBLIC_DOMAIN=ollama.example.com
OLLAMA_PRIVATE_DOMAIN=ollama.local.example.com

# OpenMemory (AI Memory Layer with UI)
OPENMEMORY_API_DOMAIN=memory-api.example.com
OPENMEMORY_API_INTERNAL_DOMAIN=memory-api.local.example.com
OPENMEMORY_UI_DOMAIN=memory.example.com
OPENMEMORY_UI_INTERNAL_DOMAIN=memory.local.example.com

# --------------------------------------------
# OpenMemory Configuration
# --------------------------------------------
# Official mem0ai OpenMemory with web dashboard
# Docs: https://docs.mem0.ai/openmemory/overview

# User identifier for memory isolation
OPENMEMORY_USER=nate

# OpenAI API key (required to start, but configure Ollama via UI after)
# Set to a dummy value if using Ollama exclusively
OPENMEMORY_OPENAI_KEY=sk-dummy-configure-ollama-via-ui

# --------------------------------------------
# Post-Deployment: Configure Ollama
# --------------------------------------------
# After starting the stack, configure Ollama via the UI or API:
#
# UI: Visit https://memory.example.com -> Settings -> LLM Config
#     Set provider to "ollama" and base URL to "http://ollama:11434"
#
# API: curl -X PUT https://memory-api.example.com/api/v1/config/mem0/llm \
#        -H "Content-Type: application/json" \
#        -d '{"provider": "ollama", "config": {"model": "llama3.2:3b", "ollama_base_url": "http://ollama:11434"}}'
#
# Similarly for embeddings:
# API: curl -X PUT https://memory-api.example.com/api/v1/config/mem0/embedder \
#        -H "Content-Type: application/json" \
#        -d '{"provider": "ollama", "config": {"model": "nomic-embed-text", "ollama_base_url": "http://ollama:11434"}}'
